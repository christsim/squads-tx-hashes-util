{
  "name": "squads-tx-hashes-util",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && mv dist/index.html dist/squads-tx-hashes.html && cd dist && shasum -a 256 squads-tx-hashes.html > squads-tx-hashes.html.sha256",
    "build:lib": "vite build --config vite.config.lib.ts && tsc -p tsconfig.lib.json",
    "build:all": "npm run build && npm run build:lib",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^4.1.0"
  }
}
